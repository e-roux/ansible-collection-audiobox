---
profile: production
offline: true

exclude_paths:
  - roles/asset_upnp/

warn_list: []

skip_list:
  - galaxy[no-changelog]
  - var-naming[no-role-prefix]
  - var-naming[pattern]
  - role-name[path]
  - name[play]
